#include "main.h"
/**
 * _isupper - The function checks for uppercase character
 * @c: is a character
 * Return: 0 or 1
 * _isupper: the function
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	else
		return (0);



}
